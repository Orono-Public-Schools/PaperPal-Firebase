rules_version = '2';

// Phase 1: lock everything down. Per-collection access opens up in Phase 2.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
